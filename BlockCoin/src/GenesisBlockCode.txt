//genesis blockchain
BlockChain bc = new BlockChain();
Transaction tr = new Transaction(new Key("0x0000000000000000"), wallet.PublicKey, 1000000, 0);
Block block = new Block(new System.Collections.Generic.List<Transaction>() { tr });
bc.AddBlock(block);

IO.SerializeBlockChain(bc);