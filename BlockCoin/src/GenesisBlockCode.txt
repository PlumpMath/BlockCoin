 //construction the genesis block
            Wallet genesisWallet = new Wallet();
            genesisWallet.Balance = 1000000;


            Wallet wallet = IO.OpenCreateWallet();
            Console.WriteLine("Your public key : {0}", wallet.PublicKey._Key);
            Console.WriteLine("Your private key : {0}", wallet.PrivateKey._Key);
            Console.WriteLine("Your balance : {0}", wallet.Balance);

            Transaction genesisTransaction = new Transaction(genesisWallet.PublicKey, wallet.PublicKey, 1000000);
            Block genesisBlock = new Block(new System.Collections.Generic.List<Transaction>() {
                genesisTransaction

                });

            BlockChain bc = new BlockChain();
            bc.AddBlock(genesisBlock);
            BlockChain.SerializeBlockChain(bc);

